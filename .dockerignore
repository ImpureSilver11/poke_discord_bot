# flyctl launch added from .gitignore
**/.env
img

# flyctl launch added from .ruby-lsp/.gitignore
.ruby-lsp/**/*
fly.toml

# test / Rakefile はビルド中の test ステージで使うので、ここでは除外しない。
# 最終イメージには Dockerfile 側で必要なものだけを COPY しているので入らない。

# ビルドにもテストにも不要なものは含めない
.github
.githooks
.git
.gitignore
.bundle
.claude
.ruby-lsp
