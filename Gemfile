source 'https://rubygems.org'

gem 'concurrent-ruby'
gem 'discordrb'
gem 'dotenv'

# 開発・テスト用。本番イメージには入れない（Dockerfile の BUNDLE_WITHOUT で除外）
group :development, :test do
  gem 'minitest'
  gem 'rake'
end
